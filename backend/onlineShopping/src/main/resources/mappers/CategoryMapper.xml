<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.online_shopping_project.onlineshopping.mapper.CategoryMapper">
    <!-- 类目字段与实体类映射 -->
    <resultMap id="CategoryMap" type="com.online_shopping_project.onlineshopping.entity.Category">
        <id     column="id"         property="id"/>
        <result column="parent_id"  property="parentId"/>
        <result column="name"       property="name"/>
        <result column="level"      property="level"/>
        <result column="sort"       property="sort"/>
        <result column="status"     property="status"/>
    </resultMap>
    <!-- 查询所有启用状态的类目 -->
    <select id="selectAllActive" resultMap="CategoryMap">
        SELECT id, parent_id, name, level, sort, status
        FROM category
        WHERE status = 1
        ORDER BY level ASC, sort ASC, id ASC
    </select>
    <!-- 根据父类目ID查询子类目 -->
    <select id="selectByParentId" parameterType="long" resultMap="CategoryMap">
        SELECT id, parent_id, name, level, sort, status
        FROM category
        WHERE status = 1 AND ( (#{_parameter} IS NULL AND parent_id IS NULL) OR parent_id = #{_parameter} )
        ORDER BY sort ASC, id ASC
    </select>

</mapper>

